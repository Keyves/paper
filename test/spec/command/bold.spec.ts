import DocumentData from 'example/mock/document.data'
import { DocumentNode } from 'src/model/node'
// import * as _ from 'lodash'

describe('bold', () => {
    let docNode: DocumentNode

    beforeEach(() => {
        docNode = new DocumentNode(DocumentData)
    })
})
